//.title
// ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
//
// GENERATED - DO NOT MODIFY BY HAND
// See: https://github.com/dev-cetera/df_generate_dart_models
//
// ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
//.title~

// ignore_for_file: annotate_overrides
// ignore_for_file: overridden_fields

part of 'show_top_message_overlay.dart';

// ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░

/// Generated class for [_ShowTopMessageOverlayProperties].
class ShowTopMessageOverlayProperties {
  //
  //
  //

  /// No description provided.
  final TextStyle? messageStyle;

  /// No description provided.
  final Widget? icon;

  /// No description provided.
  final double? iconSpacing;

  /// No description provided.
  final Color? backgroundColor;

  /// No description provided.
  final Duration? duration;

  /// No description provided.
  final EdgeInsets? padding;

  /// No description provided.
  final BorderRadius? borderRadius;

  /// No description provided.
  final double? topSpacing;

  /// No description provided.
  final Alignment? alignment;

  /// No description provided.
  final EdgeInsets? margin;

  /// No description provided.
  final BoxConstraints? constraints;

  /// Constructs a new instance of [ShowTopMessageOverlayProperties]
  /// from optional and required parameters.
  const ShowTopMessageOverlayProperties({
    this.messageStyle,
    this.icon,
    this.iconSpacing,
    this.backgroundColor,
    this.duration,
    this.padding,
    this.borderRadius,
    this.topSpacing,
    this.alignment,
    this.margin,
    this.constraints,
  });

  /// Creates a copy of this instance, replacing the specified fields.
  ShowTopMessageOverlayProperties copyWith({
    TextStyle? messageStyle,
    Widget? icon,
    double? iconSpacing,
    Color? backgroundColor,
    Duration? duration,
    EdgeInsets? padding,
    BorderRadius? borderRadius,
    double? topSpacing,
    Alignment? alignment,
    EdgeInsets? margin,
    BoxConstraints? constraints,
  }) {
    return ShowTopMessageOverlayProperties(
      messageStyle: messageStyle ?? this.messageStyle,
      icon: icon ?? this.icon,
      iconSpacing: iconSpacing ?? this.iconSpacing,
      backgroundColor: backgroundColor ?? this.backgroundColor,
      duration: duration ?? this.duration,
      padding: padding ?? this.padding,
      borderRadius: borderRadius ?? this.borderRadius,
      topSpacing: topSpacing ?? this.topSpacing,
      alignment: alignment ?? this.alignment,
      margin: margin ?? this.margin,
      constraints: constraints ?? this.constraints,
    );
  }

  /// Creates a copy of this instance, removing the specified fields.
  ShowTopMessageOverlayProperties copyWithout({
    bool messageStyle = true,
    bool icon = true,
    bool iconSpacing = true,
    bool backgroundColor = true,
    bool duration = true,
    bool padding = true,
    bool borderRadius = true,
    bool topSpacing = true,
    bool alignment = true,
    bool margin = true,
    bool constraints = true,
  }) {
    return ShowTopMessageOverlayProperties(
      messageStyle: messageStyle ? this.messageStyle : null,
      icon: icon ? this.icon : null,
      iconSpacing: iconSpacing ? this.iconSpacing : null,
      backgroundColor: backgroundColor ? this.backgroundColor : null,
      duration: duration ? this.duration : null,
      padding: padding ? this.padding : null,
      borderRadius: borderRadius ? this.borderRadius : null,
      topSpacing: topSpacing ? this.topSpacing : null,
      alignment: alignment ? this.alignment : null,
      margin: margin ? this.margin : null,
      constraints: constraints ? this.constraints : null,
    );
  }

  /// Returns the value of the [messageStyle] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  TextStyle? get messageStyle$ => messageStyle;

  /// Returns the value of the [icon] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Widget? get icon$ => icon;

  /// Returns the value of the [iconSpacing] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  double? get iconSpacing$ => iconSpacing;

  /// Returns the value of the [backgroundColor] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Color? get backgroundColor$ => backgroundColor;

  /// Returns the value of the [duration] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Duration? get duration$ => duration;

  /// Returns the value of the [padding] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  EdgeInsets? get padding$ => padding;

  /// Returns the value of the [borderRadius] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  BorderRadius? get borderRadius$ => borderRadius;

  /// Returns the value of the [topSpacing] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  double? get topSpacing$ => topSpacing;

  /// Returns the value of the [alignment] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Alignment? get alignment$ => alignment;

  /// Returns the value of the [margin] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  EdgeInsets? get margin$ => margin;

  /// Returns the value of the [constraints] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  BoxConstraints? get constraints$ => constraints;
}
