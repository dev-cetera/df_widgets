//.title
// ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
//
// GENERATED - DO NOT MODIFY BY HAND
// See: https://github.com/robmllze/df_generate_dart_models
//
// ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
//.title~

// ignore_for_file: annotate_overrides
// ignore_for_file: overridden_fields

part of 'plastic_check_btn.dart';

// ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░

/// Generated class for [_PlasticCheckBtnProperties].
class PlasticCheckBtnProperties extends _PlasticCheckBtnProperties {
  //
  //
  //

  /// No description provided.
  final double? iconSize;

  /// No description provided.
  final IconData? checkedIconData;

  /// No description provided.
  final Color? checkedIconColor;

  /// No description provided.
  final Color? checkedColor;

  /// No description provided.
  final Color? checkedShadowColor;

  /// No description provided.
  final IconData? uncheckedIconData;

  /// No description provided.
  final Color? uncheckedIconColor;

  /// No description provided.
  final Color? uncheckedColor;

  /// No description provided.
  final Color? uncheckedShadowColor;

  /// No description provided.
  final IconData? checkedHoverIconData;

  /// No description provided.
  final Color? checkedHoverIconColor;

  /// No description provided.
  final Color? checkedHoverColor;

  /// No description provided.
  final Color? checkedHoverShadowColor;

  /// No description provided.
  final IconData? uncheckedHoverIconData;

  /// No description provided.
  final Color? uncheckedHoverIconColor;

  /// No description provided.
  final Color? uncheckedHoverColor;

  /// No description provided.
  final Color? uncheckedHoverShadowColor;

  /// No description provided.
  final ExtrudedBoxProperties? extrudedBoxProperties;

  /// Constructs a new instance of [PlasticCheckBtnProperties]
  /// from optional and required parameters.
  const PlasticCheckBtnProperties({
    this.iconSize,
    this.checkedIconData,
    required this.checkedIconColor,
    required this.checkedColor,
    required this.checkedShadowColor,
    this.uncheckedIconData,
    required this.uncheckedIconColor,
    required this.uncheckedColor,
    required this.uncheckedShadowColor,
    this.checkedHoverIconData,
    required this.checkedHoverIconColor,
    required this.checkedHoverColor,
    required this.checkedHoverShadowColor,
    this.uncheckedHoverIconData,
    required this.uncheckedHoverIconColor,
    required this.uncheckedHoverColor,
    required this.uncheckedHoverShadowColor,
    this.extrudedBoxProperties,
  });

  /// Creates a copy of this instance, replacing the specified fields.
  PlasticCheckBtnProperties copyWith({
    double? iconSize,
    IconData? checkedIconData,
    Color? checkedIconColor,
    Color? checkedColor,
    Color? checkedShadowColor,
    IconData? uncheckedIconData,
    Color? uncheckedIconColor,
    Color? uncheckedColor,
    Color? uncheckedShadowColor,
    IconData? checkedHoverIconData,
    Color? checkedHoverIconColor,
    Color? checkedHoverColor,
    Color? checkedHoverShadowColor,
    IconData? uncheckedHoverIconData,
    Color? uncheckedHoverIconColor,
    Color? uncheckedHoverColor,
    Color? uncheckedHoverShadowColor,
    ExtrudedBoxProperties? extrudedBoxProperties,
  }) {
    return PlasticCheckBtnProperties(
      iconSize: iconSize ?? this.iconSize,
      checkedIconData: checkedIconData ?? this.checkedIconData,
      checkedIconColor: checkedIconColor ?? this.checkedIconColor,
      checkedColor: checkedColor ?? this.checkedColor,
      checkedShadowColor: checkedShadowColor ?? this.checkedShadowColor,
      uncheckedIconData: uncheckedIconData ?? this.uncheckedIconData,
      uncheckedIconColor: uncheckedIconColor ?? this.uncheckedIconColor,
      uncheckedColor: uncheckedColor ?? this.uncheckedColor,
      uncheckedShadowColor: uncheckedShadowColor ?? this.uncheckedShadowColor,
      checkedHoverIconData: checkedHoverIconData ?? this.checkedHoverIconData,
      checkedHoverIconColor:
          checkedHoverIconColor ?? this.checkedHoverIconColor,
      checkedHoverColor: checkedHoverColor ?? this.checkedHoverColor,
      checkedHoverShadowColor:
          checkedHoverShadowColor ?? this.checkedHoverShadowColor,
      uncheckedHoverIconData:
          uncheckedHoverIconData ?? this.uncheckedHoverIconData,
      uncheckedHoverIconColor:
          uncheckedHoverIconColor ?? this.uncheckedHoverIconColor,
      uncheckedHoverColor: uncheckedHoverColor ?? this.uncheckedHoverColor,
      uncheckedHoverShadowColor:
          uncheckedHoverShadowColor ?? this.uncheckedHoverShadowColor,
      extrudedBoxProperties:
          extrudedBoxProperties ?? this.extrudedBoxProperties,
    );
  }

  /// Creates a copy of this instance, removing the specified fields.
  PlasticCheckBtnProperties copyWithout({
    bool iconSize = true,
    bool checkedIconData = true,
    bool checkedIconColor = true,
    bool checkedColor = true,
    bool checkedShadowColor = true,
    bool uncheckedIconData = true,
    bool uncheckedIconColor = true,
    bool uncheckedColor = true,
    bool uncheckedShadowColor = true,
    bool checkedHoverIconData = true,
    bool checkedHoverIconColor = true,
    bool checkedHoverColor = true,
    bool checkedHoverShadowColor = true,
    bool uncheckedHoverIconData = true,
    bool uncheckedHoverIconColor = true,
    bool uncheckedHoverColor = true,
    bool uncheckedHoverShadowColor = true,
    bool extrudedBoxProperties = true,
  }) {
    return PlasticCheckBtnProperties(
      iconSize: iconSize ? this.iconSize : null,
      checkedIconData: checkedIconData ? this.checkedIconData : null,
      checkedIconColor: checkedIconColor ? this.checkedIconColor : null,
      checkedColor: checkedColor ? this.checkedColor : null,
      checkedShadowColor: checkedShadowColor ? this.checkedShadowColor : null,
      uncheckedIconData: uncheckedIconData ? this.uncheckedIconData : null,
      uncheckedIconColor: uncheckedIconColor ? this.uncheckedIconColor : null,
      uncheckedColor: uncheckedColor ? this.uncheckedColor : null,
      uncheckedShadowColor:
          uncheckedShadowColor ? this.uncheckedShadowColor : null,
      checkedHoverIconData:
          checkedHoverIconData ? this.checkedHoverIconData : null,
      checkedHoverIconColor:
          checkedHoverIconColor ? this.checkedHoverIconColor : null,
      checkedHoverColor: checkedHoverColor ? this.checkedHoverColor : null,
      checkedHoverShadowColor:
          checkedHoverShadowColor ? this.checkedHoverShadowColor : null,
      uncheckedHoverIconData:
          uncheckedHoverIconData ? this.uncheckedHoverIconData : null,
      uncheckedHoverIconColor:
          uncheckedHoverIconColor ? this.uncheckedHoverIconColor : null,
      uncheckedHoverColor:
          uncheckedHoverColor ? this.uncheckedHoverColor : null,
      uncheckedHoverShadowColor:
          uncheckedHoverShadowColor ? this.uncheckedHoverShadowColor : null,
      extrudedBoxProperties:
          extrudedBoxProperties ? this.extrudedBoxProperties : null,
    );
  }

  /// Returns the value of the [iconSize] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  double? get iconSize$ => iconSize;

  /// Returns the value of the [checkedIconData] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  IconData? get checkedIconData$ => checkedIconData;

  /// Returns the value of the [checkedIconColor] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Color get checkedIconColor$ => checkedIconColor!;

  /// Returns the value of the [checkedColor] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Color get checkedColor$ => checkedColor!;

  /// Returns the value of the [checkedShadowColor] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Color get checkedShadowColor$ => checkedShadowColor!;

  /// Returns the value of the [uncheckedIconData] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  IconData? get uncheckedIconData$ => uncheckedIconData;

  /// Returns the value of the [uncheckedIconColor] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Color get uncheckedIconColor$ => uncheckedIconColor!;

  /// Returns the value of the [uncheckedColor] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Color get uncheckedColor$ => uncheckedColor!;

  /// Returns the value of the [uncheckedShadowColor] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Color get uncheckedShadowColor$ => uncheckedShadowColor!;

  /// Returns the value of the [checkedHoverIconData] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  IconData? get checkedHoverIconData$ => checkedHoverIconData;

  /// Returns the value of the [checkedHoverIconColor] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Color get checkedHoverIconColor$ => checkedHoverIconColor!;

  /// Returns the value of the [checkedHoverColor] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Color get checkedHoverColor$ => checkedHoverColor!;

  /// Returns the value of the [checkedHoverShadowColor] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Color get checkedHoverShadowColor$ => checkedHoverShadowColor!;

  /// Returns the value of the [uncheckedHoverIconData] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  IconData? get uncheckedHoverIconData$ => uncheckedHoverIconData;

  /// Returns the value of the [uncheckedHoverIconColor] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Color get uncheckedHoverIconColor$ => uncheckedHoverIconColor!;

  /// Returns the value of the [uncheckedHoverColor] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Color get uncheckedHoverColor$ => uncheckedHoverColor!;

  /// Returns the value of the [uncheckedHoverShadowColor] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  Color get uncheckedHoverShadowColor$ => uncheckedHoverShadowColor!;

  /// Returns the value of the [extrudedBoxProperties] field.
  /// If the field is nullable, the return value may be null; otherwise, it
  /// will always return a non-null value.
  @pragma('vm:prefer-inline')
  ExtrudedBoxProperties? get extrudedBoxProperties$ => extrudedBoxProperties;
}
